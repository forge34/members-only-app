extends layout

block content
    form(method="POST")
        label(for="username") Username
        input(name="username")

        label(for="password") Password
        input(name="password")

        button Submit

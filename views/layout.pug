doctype html
html
  head
    title Members-only App
    link(rel='stylesheet', href='/stylesheets/main.css')
  body
    .header
          h1
            a(href="/").title Members-only App

          if !logged
            a(href="/login").push.link-btn Login
          else
            a(href="/logout").push.link-btn Log out

          a(href="/signup").link-btn Sign up

    block content
